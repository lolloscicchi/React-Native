import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    fontSize: 18,
    paddingVertical: 10,
    textAlign: 'center',
    borderWidth: 2,
    marginHorizontal: '10%',
    borderRadius: 10,
    marginBottom: 10,
  },
});
